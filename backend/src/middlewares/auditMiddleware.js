// backend/src/middlewares/auditMiddleware.js

/**
 * Middleware de auditoría - Registra acciones de usuarios
 * Por ahora es un placeholder, se puede expandir después
 */
const auditMiddleware = (req, res, next) => {
  // Placeholder para auditoría futura
  next();
};

export default auditMiddleware;